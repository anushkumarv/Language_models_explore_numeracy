## Where the samples will be written
save_data: mathqanmt
## Where the vocab(s) will be written
src_vocab: mathqanmt.vocab.src
tgt_vocab: mathqanmt.vocab.tgt
# Prevent overwriting existing files in the folder
overwrite: False

# Corpus opts:
data:
    corpus_1:
        path_src: ../data/train.json_src.txt
        path_tgt: ../data/train.json_tgt.txt
    valid:
        path_src: ../data/dev.json_src.txt
        path_tgt: ../data/dev.json_tgt.txt